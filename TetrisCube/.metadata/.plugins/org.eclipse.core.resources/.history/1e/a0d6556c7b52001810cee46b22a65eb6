package graphics;

import java.util.HashMap;

public class Test {
	public static class Tuplep {
		int x;
		int y;
		
		public Tuplep(int x, int y) {
			this.x = x;
			this.y = y;
		}

		public boolean equals(Tuplep obj) {
			return this.x == obj.x && this.y == obj.y;
		}
		
		
	}
	
	public static void main(String[] args) {
		HashMap<Tuplep, Integer> map = new HashMap<>();
		//Tuple a = new Tuple(1, 2);
		Tuplep b = new Tuplep(1, 2);
		
		
	}
}
